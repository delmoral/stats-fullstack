const smokeCountModel = require('../../models/smoke/smokeCount');

smokeCountController = {}

// Crear smokeCount para fecha nueva

// Find SmokeCount by smokeID
// Find SmokeCount by fecha y smokEID

// Añadir +1 Cigarro a fecha "hoy" - actualiza consumed de Smoke padre
// Borrar -1 Cigarro - actualiza consumed de Smoke padre

module.exports = smokeCountController;